{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "lint:fix": {
      "outputs": ["src/**"]
    },
    "dev": {
      "cache": false
    },
    "test": {
      "dependsOn": ["lint", "build"],
      "outputs": []
    }
  }
}
